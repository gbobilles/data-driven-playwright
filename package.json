{
  "name": "playwright-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Data-driven Playwright TypeScript tests reading input data from an Excel (.xlsx) file",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "xlsx": "^0.18.5"
  }
}
