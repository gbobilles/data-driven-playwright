# data-driven-playwright
Running UI tests using data driven excel file
- Visual tests

## Setup
Install [Node](https://nodejs.org/) project dependencies:

```sh
npm install
npx playwright install

## Run tests
- To run the test file in a headless
`npm run test`
- To run the visual tests
`npm run test:headed`
- To open HTML report
`npm run report`

